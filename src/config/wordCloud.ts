export const wordCloudConfig = {
  flowSpeed: 4.5,  // Global flow speed for all words
  wordGenerationRate: 100, // New word every 300ms
  titleArea: {
    width: 150,    // Width of the title avoidance area
    height: 80    // Height of the title avoidance area
  },
  wordSettings: {
    minSize: 12,   // Reduced minimum size
    maxSize: 18,   // Reduced maximum size
    opacity: 0.9   // Base opacity
  }
}; 